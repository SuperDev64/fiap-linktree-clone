export type SocialLinks = {
  instagram: string;
  youtube: string;
  linkedin: string;
  x: string;
};

export type CustomLink = {
  id: string;
  label: string;
  url: string;
};

export type GithubProfile = {
  name: string;
  avatarUrl: string;
  followers: number;
  publicRepos: number;
};
